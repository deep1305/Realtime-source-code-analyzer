from setuptools import setup, find_packages

setup(
    name="realtime-source-code-analyser-of-github",
    version="0.1.0",
    author = "Deep",
    packages = find_packages(),
    install_requires = []
)

